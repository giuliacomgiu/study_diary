<%= form_with(model:[ @study_item, @study_item.comments.build ], local: true) do |form| %>
  <%= form.label :comment, "Adicione um comentário" %><br>
  <%= form.text_area :comment %><br><br>
  <%= form.submit("Salvar") %>
<% end %>
